[runner]
# Disable Streamlit Magic so internal Python return values (such as
# DeltaGenerator objects from list comprehensions) are never rendered.
magicEnabled = false

[theme]
base = "light"
primaryColor = "#7867E8"
backgroundColor = "#F7F7FB"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#202033"
font = "sans serif"
